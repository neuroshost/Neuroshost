import {NextResponse} from "next/server";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";
export async function GET(){try{await requireAdmin();const users=await query<any[]>("SELECT id,name,email FROM users ORDER BY name ASC");const currencies=await query<any[]>("SELECT code,name,symbol,decimals FROM currencies WHERE enabled=1 ORDER BY code ASC");return NextResponse.json({users,currencies})}catch{return NextResponse.json({error:"Unauthorized"},{status:401})}}

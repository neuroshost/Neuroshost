import {NextRequest,NextResponse} from "next/server";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){const a=await requireAdmin().catch(()=>null);if(!a)return new NextResponse("Unauthorized",{status:401});const {id}=await params;await query("DELETE FROM invoice_transactions WHERE id=?",[Number(id)]);return NextResponse.redirect(new URL("/admin/invoices/transactions",req.url),303)}

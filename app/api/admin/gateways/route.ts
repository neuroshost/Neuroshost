import {NextRequest,NextResponse} from "next/server";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import {encrypt} from "@/lib/crypto";
export async function POST(req:NextRequest){try{await requireAdmin()}catch{return new NextResponse("Unauthorized",{status:401})}const f=await req.formData();const id=Number(f.get("id")||0);const name=String(f.get("name")||"").trim();const gatewayType=String(f.get("gateway_type")||"").trim();const settings:Record<string,string|boolean>={};for(const [k,v] of f.entries())if(k.startsWith("setting_"))settings[k.slice(8)]=v instanceof File?"":String(v);if(settings.verify_ssl!==undefined)settings.verify_ssl=f.get("setting_verify_ssl")!==null;const stored=encrypt(JSON.stringify(settings));if(!name||!gatewayType)return new NextResponse("Données invalides",{status:400});if(id)await query("UPDATE gateways SET name=?,gateway_type=?,settings=?,enabled=? WHERE id=?",[name,gatewayType,stored,f.get("enabled")?1:0,id]);else await query("INSERT INTO gateways(name,gateway_type,settings,enabled) VALUES(?,?,?,?)",[name,gatewayType,stored,f.get("enabled")?1:0]);return NextResponse.redirect(new URL("/admin/module/gateways",req.url),303)}

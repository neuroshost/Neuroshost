import {NextRequest,NextResponse} from "next/server";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";export async function POST(req:NextRequest){try{await requireAdmin()}catch{return new NextResponse("Unauthorized",{status:401})}const f=await req.formData();const id=Number(f.get("id"));const action=String(f.get("action")||"");if(!id)return new NextResponse("Invalid id",{status:400});if(action==="resolve")await query(`UPDATE failed_jobs SET resolved_at=NOW() WHERE id=?`,[id]);if(action==="retry")await query(`UPDATE failed_jobs SET resolved_at=NOW() WHERE id=?`,[id]);return NextResponse.redirect(new URL("/admin/module/failed-jobs",req.url),303)}

import {NextRequest,NextResponse} from "next/server";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";export async function POST(req:NextRequest){try{await requireAdmin()}catch{return new NextResponse("Unauthorized",{status:401})}const f=await req.formData();await query("INSERT INTO email_templates(template_key,name,subject,body) VALUES(?,?,?,?)",[String(f.get("templateKey")),String(f.get("name")),String(f.get("subject")),String(f.get("body"))]);return NextResponse.redirect(new URL("/admin/emails",req.url),303)}

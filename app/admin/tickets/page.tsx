import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";export const dynamic="force-dynamic";
export default async function Tickets(){try{await requireAdmin()}catch{redirect("/admin/login")}const tickets=await query<any[]>("SELECT t.*,u.name user_name,u.email FROM tickets t LEFT JOIN users u ON u.id=t.user_id ORDER BY t.id DESC");const open=tickets.filter(t=>t.status==="OPEN").length,answered=tickets.filter(t=>t.status==="ANSWERED").length,closed=tickets.filter(t=>t.status==="CLOSED").length;return <AdminShell title="Tickets"><div className="page-toolbar"><a className="btn primary" href="/admin/tickets/create">Créer</a></div><div className="stats-3"><div className="stat-card"><span>Open Tickets</span><strong>{open}</strong></div><div className="stat-card"><span>Closed Tickets</span><strong>{closed}</strong></div><div className="stat-card"><span>Replied Tickets</span><strong>{answered}</strong></div></div><div className="ticket-tabs"><a className="active" href="/admin/tickets">◉　All Tickets</a><a href="/admin/tickets?status=OPEN">▣　Open Tickets</a><a href="/admin/tickets?status=ANSWERED">▤　Replied Tickets</a><a href="/admin/tickets?status=CLOSED">▢　Closed Tickets</a></div><div className="card table-card-full"><div className="toolbar-card"><input className="input search-input" placeholder="Rechercher" /></div><table className="table admin-table"><thead><tr><th>ID</th><th>Subject</th><th>Status</th><th>Priority</th><th>Department</th><th>User</th><th></th></tr></thead><tbody>{tickets.length?tickets.map(t=><tr key={t.id}><td>#{t.id}</td><td><a href={`/admin/tickets/${t.id}`} className="action-link">{t.subject}</a></td><td><span className={t.status==="CLOSED"?"muted":"ok"}>● {t.status}</span></td><td>{t.priority||"MEDIUM"}</td><td>{t.department||"—"}</td><td>{t.user_name||"—"}</td><td className="action-cell"><a className="action-link" href={`/admin/tickets/${t.id}`}>Modifier</a></td></tr>):<tr><td colSpan={7}><div className="empty">Aucun ticket.</div></td></tr>}</tbody></table></div></AdminShell>}

import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";import InvoiceNav from "../InvoiceNav";
export const dynamic="force-dynamic";
function money(v:any){return `€${Number(v||0).toFixed(2)}`}
export default async function InvoiceTransactions(){try{await requireAdmin()}catch{redirect("/admin/login")}const rows=await query<any[]>("SELECT t.*,i.number invoice_number FROM invoice_transactions t LEFT JOIN invoices i ON i.id=t.invoice_id ORDER BY t.id DESC");return <AdminShell title="Invoice Transactions"><div className="invoice-layout"><InvoiceNav/><div className="invoice-content"><div className="invoice-card card"><div className="table-toolbar"><div className="search-wrap">⌕<input className="search-input" placeholder="Rechercher"/></div><span className="filter-icon">▥</span></div><div className="table-scroll"><table className="table invoice-table transaction-table"><thead><tr><th>□</th><th>Invoice id　⌄</th><th>Gateway　⌄</th><th>Amount　⌄</th><th>Fee　⌄</th><th>Transaction id</th><th>Status　⌄</th><th></th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r.id}><td>□</td><td>{r.invoice_id}</td><td>{r.gateway||"—"}</td><td>{money(r.amount)}</td><td>{money(r.fee)}</td><td>{r.transaction_id||"—"}</td><td><span className={`status-pill ${r.status.toLowerCase()}`}>{r.status.charAt(0)+r.status.slice(1).toLowerCase()}</span></td><td><button className="danger-link">♧ Supprimer</button></td></tr>):<tr><td colSpan={8}><div className="empty">Aucune transaction.</div></td></tr>}</tbody></table></div><div className="table-footer"><span>Affichage de 1 à {rows.length} sur {rows.length} résultats</span><span className="page-size">par page　10　⌄</span></div></div></div></div></AdminShell>}

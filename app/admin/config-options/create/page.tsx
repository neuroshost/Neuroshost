import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";import HtmlEditor from "@/components/HtmlEditor";
export const dynamic="force-dynamic";
export default async function CreateConfigOption(){try{await requireAdmin()}catch{redirect("/admin/login")}const products=await query<any[]>("SELECT id,name FROM products ORDER BY name");return <AdminShell title="Créer Config Option"><form className="module-form single-panel" action="/api/admin/config-options" method="post"><div className="form-tab">General</div><div className="editor-body"><label>Name*<input className="input" name="name" required placeholder="Enter the name of the configuration option"/></label><label>Description<HtmlEditor name="description" placeholder="Description HTML de l'option de configuration..."/></label><label>Environment Variable<input className="input" name="environmentVariable" placeholder="Enter the environment variable name"/></label><label>Type*<select className="input" name="type" required defaultValue="TEXT"><option value="TEXT">Text</option><option value="NUMBER">Number</option><option value="BOOLEAN">Boolean</option><option value="SELECT">Select</option><option value="PASSWORD">Password</option></select></label><label className="checkline"><input type="checkbox" name="hidden" value="1"/> Hidden</label><label>Products<select className="input multi-select" name="productIds" multiple>{products.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></label><div className="form-actions"><button className="btn primary">Créer</button><button className="btn" name="createAnother" value="1">Créer & Ajouter un autre</button><a className="btn" href="/admin/config-options">Annuler</a></div></div></form></AdminShell>}

import { redirect } from "next/navigation"; import { isInstalled } from "@/lib/runtime"; import { readSession } from "@/lib/auth";
export default async function Login(){ if(!isInstalled())redirect("/install"); if(await readSession())redirect("/admin"); return <div className="container" style={{maxWidth:520,paddingTop:80}}><div className="card"><div className="brand">NEUROS<span>HOST</span></div><h1>Administration</h1><form className="form" action="/api/auth/login" method="post"><label>Email<input className="input" type="email" name="email" required/></label><label>Mot de passe<input className="input" type="password" name="password" required/></label><button className="btn primary">Se connecter</button></form></div></div> }
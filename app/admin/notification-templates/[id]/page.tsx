import {notFound,redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";import NotificationTemplateForm from "../NotificationTemplateForm";export const dynamic="force-dynamic";export default async function EditNotificationTemplate({params}:{params:Promise<{id:string}>}){try{await requireAdmin()}catch{redirect("/admin/login")}const{id}=await params;const rows=await query<any[]>("SELECT * FROM email_templates WHERE id=? LIMIT 1",[id]);if(!rows[0])notFound();return <AdminShell title="Modifier Notification Template"><NotificationTemplateForm template={rows[0]}/></AdminShell>}

import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import AdminShell from "@/components/AdminShell";import NotificationTemplateForm from "../NotificationTemplateForm";export const dynamic="force-dynamic";export default async function NewNotificationTemplate(){try{await requireAdmin()}catch{redirect("/admin/login")}return <AdminShell title="Créer Notification Template"><NotificationTemplateForm/></AdminShell>}

"use client";
import {useMemo,useState} from "react";
export default function NotificationTemplateList({templates}:{templates:any[]}){const[search,setSearch]=useState("");const filtered=useMemo(()=>templates.filter(t=>`${t.name} ${t.subject} ${t.template_key}`.toLowerCase().includes(search.toLowerCase())),[templates,search]);return <><div className="topline"><div><p className="muted">Gérez les emails et notifications envoyés automatiquement par Neuroshost Billing.</p></div><a className="btn primary" href="/admin/notification-templates/new">Créer</a></div><div className="card notification-list-card"><div className="template-toolbar"><input className="input search-input" value={search} onChange={e=>setSearch(e.target.value)} placeholder="⌕  Rechercher"/></div><table className="table notification-table"><thead><tr><th>Subject</th><th>Enabled</th><th></th></tr></thead><tbody>{filtered.map(t=><tr key={t.id}><td><div>{t.subject}</div><small className="muted"><code>{t.template_key}</code></small></td><td>{t.enabled?<span className="enabled-dot">✓</span>:<span className="disabled-dot">×</span>}</td><td className="action-cell"><a href={`/admin/notification-templates/${t.id}`}>✎ Modifier</a></td></tr>)}{!filtered.length&&<tr><td colSpan={3}><div className="empty">Aucun modèle trouvé.</div></td></tr>}</tbody></table><div className="table-footer"><span>Affichage de {filtered.length} sur {templates.length} résultats</span><span className="page-size">par page&nbsp;&nbsp;25⌄</span></div></div></>}

import {redirect,notFound} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";import {RoleForm} from "@/components/ModuleForms";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){try{await requireAdmin()}catch{redirect("/admin/login")}const{id}=await params;const r=(await query<any[]>("SELECT * FROM roles WHERE id=?",[Number(id)]))[0];if(!r)notFound();try{r.permissions=JSON.parse(r.permissions||"[]")}catch{r.permissions=[]}return <AdminShell title="Modifier Role"><RoleForm initial={r}/></AdminShell>}

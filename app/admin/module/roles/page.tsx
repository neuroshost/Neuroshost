import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";export const dynamic="force-dynamic";export default async function Page(){try{await requireAdmin()}catch{redirect("/admin/login")}const rows=await query<any[]>("SELECT * FROM roles ORDER BY id DESC");return <AdminShell title="Roles"><div className="page-toolbar"><a className="btn primary" href="/admin/module/roles/create">Créer</a></div><div className="card table-card-full"><table className="table admin-table"><thead><tr><th>Name</th><th>permissions</th><th></th></tr></thead><tbody>{rows.map(r=>{let p:any[]=[];try{p=JSON.parse(r.permissions||"[]")}catch{}return <tr key={r.id}><td>{r.name}</td><td>{p.includes("All Permissions")?<b>All</b>:p.length}</td><td className="action-cell"><a className="action-link" href={`/admin/module/roles/${r.id}`}>✎ Modifier</a></td></tr>})}</tbody></table><div className="table-footer">Affichage de {rows.length} résultat{rows.length>1?"s":""}<span className="page-size">par page &nbsp; 10⌄</span></div></div></AdminShell>}

import AdminShell from "@/components/AdminShell";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";export const dynamic="force-dynamic";
export default async function AuditsPage(){await requireAdmin();const rows=await query<any[]>(`SELECT id,user_name,event,ip_address,details,created_at FROM audit_logs ORDER BY id DESC LIMIT 100`);return <AdminShell title="Audits"><div className="module-table-card card"><div className="toolbar-card"><input className="input search-input" placeholder="Rechercher"/></div><div className="table-scroll"><table className="table admin-table"><thead><tr><th>User id</th><th>Event</th><th>Ip address</th><th>Date</th><th>Action</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.user_name||"System"}</td><td>{r.event}</td><td>{r.ip_address||"—"}</td><td>{new Date(r.created_at).toLocaleString("fr-FR")}</td><td><span className="muted">◉ Voir</span></td></tr>)}</tbody></table>{rows.length===0&&<div className="empty">Aucun audit enregistré.</div>}</div><div className="table-footer">Affichage de 1 à {rows.length} résultats</div></div></AdminShell>}

import {redirect,notFound} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import {decrypt} from "@/lib/crypto";import AdminShell from "@/components/AdminShell";import {GatewayForm} from "@/components/ModuleForms";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){try{await requireAdmin()}catch{redirect("/admin/login")}const{id}=await params;const r=(await query<any[]>("SELECT * FROM gateways WHERE id=?",[Number(id)]))[0];if(!r)notFound();try{r.settings=decrypt(r.settings||"")}catch{r.settings="{}"}return <AdminShell title="Modifier Gateway"><GatewayForm initial={r}/></AdminShell>}

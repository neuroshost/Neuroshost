"use client";import {useState} from "react";export default function OAuthClientCreate(){const[open,setOpen]=useState(false);if(!open)return <button className="btn primary" onClick={()=>setOpen(true)}>Créer</button>;return <div className="modal-backdrop"><form className="modal-card form" action="/api/admin/oauth-clients" method="post"><div className="modal-title"><h2>Créer OAuth Client</h2><button type="button" className="icon-close" onClick={()=>setOpen(false)}>×</button></div><label>Name*<input className="input" name="name" required autoFocus/></label><label>Redirect*<input className="input" name="redirectUri" placeholder="https://example.com/oauth/callback" required/></label><div className="form-actions"><button className="btn primary">Créer</button><button type="button" className="btn" onClick={()=>setOpen(false)}>Annuler</button></div></form></div>}

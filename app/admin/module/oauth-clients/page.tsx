import AdminShell from "@/components/AdminShell";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import OAuthClientCreate from "./OAuthClientCreate";export const dynamic="force-dynamic";
export default async function OAuthClientsPage(){await requireAdmin();const rows=await query<any[]>(`SELECT id,name,client_id,redirect_uri,created_at FROM oauth_clients ORDER BY id DESC`);return <AdminShell title="OAuth Clients"><div className="system-head"><div><h2>OAuth Clients</h2><p className="muted">Applications autorisées à utiliser l'OAuth de Neuroshost Billing.</p></div><OAuthClientCreate/></div><div className="card module-table-card"><table className="table admin-table"><thead><tr><th>ID</th><th>Name</th><th>Redirect</th><th>Created</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.id}</td><td>{r.name}<div className="muted small">{r.client_id}</div></td><td>{r.redirect_uri}</td><td>{new Date(r.created_at).toLocaleString("fr-FR")}</td></tr>)}</tbody></table>{!rows.length&&<div className="empty">Aucun(e) OAuth Clients</div>}</div></AdminShell>}

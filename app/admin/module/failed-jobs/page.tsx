import AdminShell from "@/components/AdminShell";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";export const dynamic="force-dynamic";
export default async function FailedJobsPage(){await requireAdmin();const rows=await query<any[]>(`SELECT * FROM failed_jobs WHERE resolved_at IS NULL ORDER BY failed_at DESC`);return <AdminShell title="Failed Jobs"><div className="card module-table-card"><table className="table admin-table"><thead><tr><th>Payload</th><th>Exception</th><th>Failed at</th><th>Actions</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td className="code">{r.job_name}<br/>{String(r.payload||"").slice(0,180)}</td><td className="code">{String(r.exception||"").slice(0,280)}</td><td>{new Date(r.failed_at).toLocaleString("fr-FR")}</td><td><form action="/api/admin/failed-jobs" method="post" className="inline-actions"><input type="hidden" name="id" value={r.id}/><button className="link-button" name="action" value="retry">Retry</button><button className="link-button" name="action" value="resolve">Mark as Resolved</button></form></td></tr>)}</tbody></table>{!rows.length&&<div className="empty">Aucun failed job.</div>}</div></AdminShell>}

import AdminShell from "@/components/AdminShell";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import ApiKeyCreate from "./ApiKeyCreate";export const dynamic="force-dynamic";
export default async function ApiKeysPage(){await requireAdmin();const rows=await query<any[]>(`SELECT id,name,allowed_ips,enabled,last_used_at,created_at FROM api_keys ORDER BY id DESC`);return <AdminShell title="Api Keys"><div className="system-head"><div><h2>Api Keys</h2><p className="muted">Gérez les clés d'accès à l'API de Neuroshost Billing.</p></div><ApiKeyCreate/></div><div className="card module-table-card"><table className="table admin-table"><thead><tr><th>Name</th><th>Allowed IP Addresses</th><th>Enabled</th><th>Last Used At</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.name}</td><td>{r.allowed_ips||"All"}</td><td>{r.enabled?<span className="ok">● Enabled</span>:<span className="muted">○ Disabled</span>}</td><td>{r.last_used_at?new Date(r.last_used_at).toLocaleString("fr-FR"):"Never"}</td></tr>)}</tbody></table>{!rows.length&&<div className="empty">Aucun(e) api keys</div>}</div></AdminShell>}

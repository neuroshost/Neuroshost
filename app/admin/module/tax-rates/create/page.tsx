import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import AdminShell from "@/components/AdminShell";import {TaxRateForm} from "@/components/ModuleForms";export const dynamic="force-dynamic";export default async function Page(){try{await requireAdmin()}catch{redirect("/admin/login")}return <AdminShell title="Créer Tax Rate"><TaxRateForm/></AdminShell>}

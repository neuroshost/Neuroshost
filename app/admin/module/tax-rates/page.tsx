import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";export const dynamic="force-dynamic";export default async function Page(){try{await requireAdmin()}catch{redirect("/admin/login")}const rows=await query<any[]>("SELECT * FROM tax_rates ORDER BY id DESC");return <AdminShell title="Tax Rates"><div className="page-toolbar"><a className="btn primary" href="/admin/module/tax-rates/create">Créer</a></div><div className="card table-card-full"><div className="toolbar-card"><input className="input search-input" placeholder="⌕  Rechercher" readOnly/></div><table className="table admin-table"><thead><tr><th>□</th><th>Name</th><th>Rate</th><th>Country</th><th></th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r.id}><td>□</td><td>{r.name}</td><td>{Number(r.rate).toFixed(2)}</td><td>{r.country}</td><td className="action-cell"><a className="action-link" href={`/admin/module/tax-rates/${r.id}`}>✎ Modifier</a></td></tr>):<tr><td colSpan={5}><div className="empty">Aucun tax rate.</div></td></tr>}</tbody></table><div className="table-footer">Affichage de {rows.length} résultat{rows.length>1?"s":""}<span className="page-size">par page &nbsp; 10⌄</span></div></div></AdminShell>}

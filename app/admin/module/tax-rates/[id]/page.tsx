import {redirect,notFound} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";import {TaxRateForm} from "@/components/ModuleForms";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){try{await requireAdmin()}catch{redirect("/admin/login")}const{id}=await params;const r=(await query<any[]>("SELECT * FROM tax_rates WHERE id=?",[Number(id)]))[0];if(!r)notFound();return <AdminShell title="Modifier Tax Rate"><TaxRateForm initial={r}/></AdminShell>}

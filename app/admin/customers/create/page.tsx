import { redirect } from "next/navigation";
import { requireAdmin } from "@/lib/auth";
import AdminShell from "@/components/AdminShell";
export const dynamic="force-dynamic";
export default async function CreateUser(){try{await requireAdmin()}catch{redirect("/admin/login")};return <AdminShell title="Créer User"><form className="user-create-form" action="/api/admin/users" method="post"><div className="two"><label>First name*<input className="input" name="firstName" required /></label><label>Last name*<input className="input" name="lastName" required /></label></div><div className="two"><label>Email*<input className="input" type="email" name="email" required /></label><label>Password*<input className="input" type="password" name="password" minLength={8} required /></label></div><label>Role<select className="input" name="role" defaultValue="CLIENT"><option value="CLIENT">Client</option><option value="STAFF">Staff</option><option value="ADMIN">Admin</option><option value="SUPER_ADMIN">Super Admin</option></select></label><div className="form-actions"><button className="btn primary">Créer</button><button className="btn" type="submit" name="continue" value="1">Créer &amp; Ajouter un autre</button><a className="btn" href="/admin/customers">Annuler</a></div></form></AdminShell>}

import {notFound,redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";export const dynamic="force-dynamic";
export default async function EditUser({params}:{params:Promise<{id:string}>}){try{await requireAdmin()}catch{redirect("/admin/login")}const {id}=await params;const rows=await query<any[]>("SELECT id,name,email,role,discord_id FROM users WHERE id=?",[Number(id)]);const u=rows[0];if(!u)notFound();const parts=String(u.name||"").split(" ");const first=parts.shift()||"";const last=parts.join(" ");return <AdminShell title={`Modifier User #${u.id}`}><form className="user-create-form" action={`/api/admin/users/${u.id}`} method="post"><div className="two"><label>First name*<input className="input" name="firstName" defaultValue={first} required/></label><label>Last name*<input className="input" name="lastName" defaultValue={last} required/></label></div><div className="two"><label>Email*<input className="input" type="email" name="email" defaultValue={u.email} required/></label><label>Password<input className="input" type="password" name="password" minLength={8} placeholder="Laisser vide pour conserver le mot de passe"/></label></div><div className="two"><label>Role<select className="input" name="role" defaultValue={u.role}><option value="CLIENT">Client</option><option value="STAFF">Staff</option><option value="ADMIN">Admin</option><option value="SUPER_ADMIN">Super Admin</option></select></label><label>Discord ID<input className="input" name="discordId" defaultValue={u.discord_id||""}/></label></div><div className="form-actions"><button className="btn primary">Enregistrer</button><a className="btn" href="/admin/customers">Annuler</a></div></form></AdminShell>}

import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";export const dynamic="force-dynamic";
export default async function Customers(){try{await requireAdmin()}catch{redirect("/admin/login")}const rows=await query<any[]>("SELECT id,name,email,role,discord_id,created_at FROM users ORDER BY id DESC");return <AdminShell title="Users"><div className="page-toolbar"><a className="btn primary" href="/admin/customers/create">Créer</a></div><div className="card table-card-full"><div className="toolbar-card"><input className="input search-input" placeholder="Rechercher" /></div><table className="table admin-table"><thead><tr><th>First name</th><th>Last name</th><th>Email</th><th>Role</th><th>Created at</th><th></th></tr></thead><tbody>{rows.length?rows.map(r=>{const parts=String(r.name||"").split(" ");const first=parts.shift()||"";const last=parts.join(" ");return <tr key={r.id}><td>{first}</td><td>{last}</td><td>{r.email}</td><td>{r.role}</td><td>{new Date(r.created_at).toLocaleString("fr-FR")}</td><td className="action-cell"><a className="action-link" href={`/admin/customers/${r.id}`}>Modifier</a></td></tr>}):<tr><td colSpan={6}><div className="empty">Aucun utilisateur.</div></td></tr>}</tbody></table><div className="table-footer"><span>Affichage de 1 à {rows.length} sur {rows.length} résultats</span><span className="page-size">par page　10⌄</span></div></div></AdminShell>}

import {redirect,notFound} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";import HtmlEditor from "@/components/HtmlEditor";
export const dynamic="force-dynamic";
export default async function EditCategory({params}:{params:Promise<{id:string}>}){try{await requireAdmin()}catch{redirect("/admin/login")}const {id}=await params;const rows=await query<any[]>("SELECT * FROM categories WHERE id=?",[Number(id)]);if(!rows[0])notFound();const c=rows[0];const parents=await query<any[]>("SELECT id,name FROM categories WHERE id<>? ORDER BY name",[Number(id)]);return <AdminShell title="Modifier Category"><form className="module-form" action={`/api/admin/categories/${id}`} method="post"><input type="hidden" name="_action" value="update"/><div className="two"><label>Name*<input className="input" name="name" defaultValue={c.name} required/></label><label>Slug*<input className="input" name="slug" defaultValue={c.slug} required/></label></div><div className="two"><label>Description<HtmlEditor name="description" defaultValue={c.description||""}/></label><label>Parent Category<select className="input" name="parentCategoryId" defaultValue={c.parent_category_id||""}><option value="">Sélectionnez une option</option>{parents.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></label></div><div className="two"><label>Image<input className="input" name="imageUrl" defaultValue={c.image_url||""}/></label><label>Sort order<input className="input" name="sortOrder" type="number" defaultValue={c.sort_order??0}/></label></div><label className="checkline"><input type="checkbox" name="visible" value="1" defaultChecked={c.visible===1}/> Visible</label><div className="form-actions"><button className="btn primary">Enregistrer</button><button className="btn danger" name="_action" value="delete">Supprimer</button><a className="btn" href="/admin/categories">Annuler</a></div></form></AdminShell>}

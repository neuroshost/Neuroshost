import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";import HtmlEditor from "@/components/HtmlEditor";
export const dynamic="force-dynamic";
export default async function CreateCategory(){try{await requireAdmin()}catch{redirect("/admin/login")}const parents=await query<any[]>("SELECT id,name FROM categories ORDER BY name");return <AdminShell title="Créer Category"><form className="module-form" action="/api/admin/categories" method="post"><div className="two"><label>Name*<input className="input" name="name" required/></label><label>Slug*<input className="input" name="slug" required/></label></div><div className="two"><label>Description<HtmlEditor name="description" placeholder="Collez votre code HTML de description..."/></label><label>Parent Category<select className="input" name="parentCategoryId"><option value="">Sélectionnez une option</option>{parents.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></label></div><div className="two"><label>Image<input className="input" name="imageUrl" placeholder="URL de l'image (optionnel)"/></label><label>Sort order<input className="input" name="sortOrder" type="number" defaultValue="0"/></label></div><label className="checkline"><input type="checkbox" name="visible" value="1" defaultChecked/> Visible</label><div className="form-actions"><button className="btn primary">Créer</button><button className="btn" name="createAnother" value="1">Créer & Ajouter un autre</button><a className="btn" href="/admin/categories">Annuler</a></div></form></AdminShell>}

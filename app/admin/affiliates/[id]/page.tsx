import {notFound,redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";export const dynamic="force-dynamic";
export default async function EditAffiliate({params}:{params:Promise<{id:string}>}){try{await requireAdmin()}catch{redirect("/admin/login")}const {id}=await params;const rows=await query<any[]>("SELECT a.*,u.name user_name,u.email FROM affiliates a JOIN users u ON u.id=a.user_id WHERE a.id=?",[Number(id)]);const a=rows[0];if(!a)notFound();const users=await query<any[]>("SELECT id,name,email FROM users ORDER BY name");return <AdminShell title={`Modifier Affiliate #${a.id}`}><div className="modal-card"><h2>Modifier Affiliate</h2><form className="form" action={`/api/admin/affiliates/${a.id}`} method="post"><label className="toggle-row"><input type="checkbox" name="enabled" value="1" defaultChecked={a.enabled===1}/> Enabled</label><div className="two"><label>User*<select className="input" name="userId" defaultValue={a.user_id} required>{users.map(u=><option key={u.id} value={u.id}>{u.name} — {u.email}</option>)}</select></label><label>Referral Code*<input className="input" name="referralCode" defaultValue={a.referral_code} required/></label></div><label>Affiliate Reward<div className="suffix-input"><input className="input" type="number" name="rewardPercent" min="0" max="100" step="0.01" defaultValue={a.reward_percent}/><span>%</span></div></label><div className="form-actions"><button className="btn primary">Enregistrer</button><a className="btn" href="/admin/affiliates">Annuler</a></div></form></div></AdminShell>}

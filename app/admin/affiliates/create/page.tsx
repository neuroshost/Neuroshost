import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";export const dynamic="force-dynamic";
export default async function CreateAffiliate(){try{await requireAdmin()}catch{redirect("/admin/login")}const users=await query<any[]>("SELECT id,name,email FROM users ORDER BY name");return <AdminShell title="Créer Affiliate"><div className="modal-card"><h2>Créer Affiliate</h2><form className="form" action="/api/admin/affiliates" method="post"><label className="toggle-row"><input type="checkbox" name="enabled" value="1" defaultChecked/> Enabled</label><div className="two"><label>User*<select className="input" name="userId" required><option value="">Sélectionnez une option</option>{users.map(u=><option key={u.id} value={u.id}>{u.name} — {u.email}</option>)}</select></label><label>Referral Code*<input className="input" name="referralCode" required/></label></div><label>Affiliate Reward<div className="suffix-input"><input className="input" type="number" name="rewardPercent" min="0" max="100" step="0.01" defaultValue="10"/><span>%</span></div><small className="muted">Percentage of the purchase amount the affiliated user would receive as a reward.</small></label><div className="form-actions"><button className="btn primary">Créer</button><button className="btn" type="submit" name="continue" value="1">Créer &amp; Ajouter un autre</button><a className="btn" href="/admin/affiliates">Annuler</a></div></form></div></AdminShell>}

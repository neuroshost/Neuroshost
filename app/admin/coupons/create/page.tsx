import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";
export const dynamic="force-dynamic";
export default async function CreateCoupon(){try{await requireAdmin()}catch{redirect("/admin/login")}const products=await query<any[]>("SELECT id,name FROM products ORDER BY name");return <AdminShell title="Créer Coupon"><form className="module-form" action="/api/admin/coupons" method="post"><div className="two"><label>Code*<input className="input" name="code" required placeholder="Enter the code of the coupon"/></label><label>Value*<div className="input-addon"><input className="input" name="value" type="number" step="0.01" required placeholder="Enter the value of the coupon"/><span>%</span></div></label></div><div className="two"><label>Type*<select className="input" name="type" defaultValue="PERCENTAGE"><option value="PERCENTAGE">Percentage</option><option value="FIXED">Fixed amount</option></select></label><label>Applies To*<select className="input" name="appliesTo" defaultValue="PRICE_SETUP"><option value="PRICE_SETUP">Price and Setup Fee</option><option value="PRICE">Price</option><option value="SETUP_FEE">Setup Fee</option></select></label></div><div className="two"><label>Recurring<input className="input" name="recurring" type="number" min="0" placeholder="How many billing cycles the discount will be applied"/><small className="muted">0 = tous les cycles, 1 = uniquement le premier cycle.</small></label><label>Max Uses<input className="input" name="maxUses" type="number" min="1" placeholder="Enter the maximum number of total uses of the coupon"/></label></div><div className="two"><label>Max Uses Per User<input className="input" name="maxUsesPerUser" type="number" min="1" placeholder="Enter the maximum number of uses per user"/></label><label>Starts At<input className="input" name="startsAt" type="datetime-local"/></label></div><div className="two"><label>Expires At<input className="input" name="expiresAt" type="datetime-local"/></label><label>Products<select className="input multi-select" name="productIds" multiple>{products.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select><small className="muted">Laissez vide pour appliquer le coupon à tous les produits.</small></label></div><div className="form-actions"><button className="btn primary">Créer</button><button className="btn" name="createAnother" value="1">Créer & Ajouter un autre</button><a className="btn" href="/admin/coupons">Annuler</a></div></form></AdminShell>}

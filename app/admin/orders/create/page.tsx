import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";import OrderForm from "@/components/OrderForm";
export const dynamic="force-dynamic";
export default async function CreateOrder(){try{await requireAdmin()}catch{redirect("/admin/login")}const users=await query<any[]>("SELECT id,name,email FROM users ORDER BY name");const products=await query<any[]>("SELECT id,name,price,currency FROM products WHERE hide_product=0 ORDER BY name");const currencies=await query<any[]>("SELECT code,name,symbol FROM currencies WHERE enabled=1 ORDER BY code");const plans=await query<any[]>("SELECT id,product_id,name,price,billing_cycle FROM product_plans WHERE enabled=1 ORDER BY sort_order,id");return <AdminShell title="Créer Order"><OrderForm users={users} products={products} currencies={currencies} plans={plans}/></AdminShell>}
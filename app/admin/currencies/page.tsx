import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";
export const dynamic="force-dynamic";
export default async function Currencies(){try{await requireAdmin()}catch{redirect("/admin/login")}const rows=await query<any[]>("SELECT * FROM currencies ORDER BY code");return <AdminShell title="Currencies"><div className="page-head"><div/><a className="btn primary" href="/admin/currencies/create">Créer</a></div><div className="card module-table-card"><div className="toolbar-card"><input className="input search-input" placeholder="Rechercher"/></div><table className="table admin-table"><thead><tr><th>Code</th><th>Prefix</th><th>Suffix</th><th>Format</th><th>Enabled</th><th></th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.code}</b></td><td>{r.prefix||r.symbol||""}</td><td>{r.suffix||""}</td><td>{r.format||"1,000.00"}</td><td>{r.enabled?"Yes":"No"}</td><td className="action-cell"><a className="action-link" href={`/admin/currencies/${r.id}`}>✎ Modifier</a></td></tr>)}</tbody></table>{!rows.length&&<div className="empty">Aucune currency</div>}<div className="table-footer">Affichage de {rows.length} résultat{rows.length>1?"s":""}<span className="page-size">par page　10⌄</span></div></div></AdminShell>}

import {redirect,notFound} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {query} from "@/lib/db";import AdminShell from "@/components/AdminShell";
export default async function EditCurrency({params}:{params:Promise<{id:string}>}){try{await requireAdmin()}catch{redirect("/admin/login")}const {id}=await params;const rows=await query<any[]>("SELECT * FROM currencies WHERE id=?",[Number(id)]);if(!rows[0])notFound();const c=rows[0];return <AdminShell title="Modifier Currency"><form className="module-form" action={`/api/admin/currencies/${id}`} method="post"><input type="hidden" name="_action" value="update"/><div className="two"><label>Code*<input className="input" name="code" maxLength={3} defaultValue={c.code} required/></label><label>Name*<input className="input" name="name" defaultValue={c.name} required/></label></div><div className="two"><label>Prefix<input className="input" name="prefix" defaultValue={c.prefix||c.symbol||""}/></label><label>Suffix<input className="input" name="suffix" defaultValue={c.suffix||""}/></label></div><div className="two"><label>Format<select className="input" name="format" defaultValue={c.format||"1,000.00"}><option value="1,000.00">1,000.00</option><option value="1.000,00">1.000,00</option><option value="1 000,00">1 000,00</option><option value="1 000.00">1 000.00</option></select></label><label>Decimals<input className="input" name="decimals" type="number" min="0" max="4" defaultValue={c.decimals??2}/></label></div><label className="checkline"><input type="checkbox" name="enabled" value="1" defaultChecked={c.enabled===1}/> Enabled</label><div className="form-actions"><button className="btn primary">Enregistrer</button><button className="btn danger" name="_action" value="delete">Supprimer</button><a className="btn" href="/admin/currencies">Annuler</a></div></form></AdminShell>}

"use client";
import { useState } from "react";
export default function PaymentMethodModal(){const [open,setOpen]=useState(false);const [gateway,setGateway]=useState("Stripe");return <>{<button className="client-primary-btn" type="button" onClick={()=>setOpen(true)}>+ Ajouter un mode de paiement</button>}{open&&<div className="client-modal-backdrop" role="dialog" aria-modal="true"><div className="client-payment-modal"><button className="client-modal-close" type="button" onClick={()=>setOpen(false)}>×</button><h2>Modes de paiement</h2><label>Passerelle de paiement <em>*</em><select className="input" id="payment-gateway" value={gateway} onChange={e=>setGateway(e.target.value)}><option>Stripe</option><option>Paypal</option></select></label><form action="/api/client/account/payment-methods" method="post"><input type="hidden" name="gateway" value={gateway}/><button className="client-primary-btn client-modal-submit" type="submit">Configurer un nouveau mode de paiement</button></form></div></div>}</>}
